package Programmers.Lv2;

public class Lv2타켓넘버 {
    /*
    class Solution {
    static int answer, number;
    static int[] arr;
    public int solution(int[] numbers, int target) {
        answer = 0;
        number = target;
        arr = numbers;
        DFS(0,0);
        return answer;
    }
    public void DFS(int cnt, int result){
        if(cnt==arr.length){
            if(result==number) answer++;
            return;
        }
            DFS(cnt+1, result+arr[cnt]);
            DFS(cnt+1, result-arr[cnt]);
    }
}
    */
}
